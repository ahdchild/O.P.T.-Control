byte currentMenu
byte totalMenus
byte currentMenuSize
byte currentItem
int currentItemMax
int currentitemMin
byte currentPage – first menuitem on the screen

PrintMenu(byte whichItem, bool isActive) Print a particular menu line to lcd. Use case switch to pull from a const String array then print the relevant variable.
SetValue(int value) Set value for a current menu item first check the value against currentItemMax and currentitemMin, then use switch case to set whichever global variable
DoAction() Do action related to current menu item.
SetMenu(byte whichMenu) change the current menu. Check against totalMenus. Update currentMenuSize
SetItem(byte whichItem) change the currentItem. Update currentItemMax and currentitemMin
PrintScreen() clear the screen. Print each line starting from currentPage



Buttons layout:

Menu up
Menu down

item up
item down

enter

measure

shoot
cancel